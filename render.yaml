services:
  - type: web
    name: maa-bhavani-construction
    runtime: node
    buildCommand: npm install; npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false   # Set this manually in Render dashboard → Environment
